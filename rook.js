import { board } from "./chess.js";

export const rookCheck = (color, pos_x, pos_y) => {
    if (color === 'w') {

        
        let move1, move2, move3, move4;

    } else {
        console.log('Black');
    }
}
